views:
  - name: best_score

    cubes:
      - join_path: round_summary
        includes:
          # MEASURE
          - best_score

          # TIME
          - play_date

          # DIMENSIONS
          - play_year
          - course_name
          - score_range